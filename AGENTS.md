# 開発ガイド

Claude Code・Codexで共有するskillsとhooksの配布元。導入・更新・hookの契約は [README](README.md)の該当節を読む。

## 入口と変更条件

- スキルの実体は `skills/<名前>/SKILL.md`。プラグイン内で共有し、利用側に手順や実装をコピーしない。
- hookは `hooks/hooks.json` と `hooks/scripts/`。変更時は [編集後のhooks](README.md#編集後フックpost-edit)の入出力・対象・制限を確認し、ClaudeとCodexの両方への影響を検証する。
- 配布設定は `.claude-plugin/marketplace.json` と両ツールの `plugin.json`（`.claude-plugin/`・`.codex-plugin/`）。配布仕様や公開手順を変える場合は [更新](README.md#プラグインの更新)も確認し、両manifestのversionをそろえる。
- 共有設定の変更は複数リポジトリに影響する。特定リポジトリの検証はそのリポジトリの設定に任せ、共通hookに固有のコマンドを増やさない。権限・マージ・公開の許可は、skillの存在や導入だけでは追加されない。

## スキルの書き方

- `description` は用途と適用条件を短く書く。関連語が出ただけで広く発火する条件や、例の列挙は避ける。
- 本文には判断に必要な固有の制約を残す。作業別の詳細は、読む条件を添えて資料に分ける。短いskillは無理に分割しない。
- 一般的な助言や同じチェックリストを重ねず、手順・注意点・完了条件の重複をまとめる。許可された作業範囲と、実行を止める条件は維持する。

## 検証

- 変更したシェルは `bash -n <対象.sh>`、JSONは `jq empty <対象.json>` で構文確認する。hookの挙動変更は一時ディレクトリと模擬コマンドで対象・除外・入出力を確かめ、利用者のリポジトリを検証用に書き換えない。
- 文書・スキル変更は手順の実行条件、相対リンク、両ツールでの扱いを確認する。作業に応じた `verify-changes` を使う。プラグイン変更に伴う導入・更新は、依頼範囲とREADMEに従う。

## 作業の進め方

- 対象のファイル・見出し・シンボルから調べ、必要な場合だけ範囲を広げる。資料やskillsは作業に該当するものを読む。
- 不明点は質問して解消してから、その判断に依存する作業に進む。すでに決まっている事項は再確認しない。
- 文書の言語を保ち、日本語は日本人に、英語は英語圏の読者に自然に伝わる表現にする。
- 必須検証は適用条件に従って実行し、同じ差分・依存・設定・実行条件で得た結果は再利用する。問題を修正し、結果と未確認の範囲を簡潔に報告する。
- このガイドには継続して必要な規約と参照先を残す。進捗や設定値、他の資料・skillsの手順は複製しない。
