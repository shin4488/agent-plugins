# 開発ガイド

Claude Code・Codexで共有するskillsとhooksの配布元。導入・更新・hookの契約は [README](README.md)の該当節を読む。

## 入口と変更条件

- スキルの実体は `skills/<名前>/SKILL.md`。プラグイン内で共有し、利用側に手順や実装をコピーしない。
- hookは `hooks/hooks.json` と `hooks/scripts/`。変更時は [編集後のhooks](README.md#編集後のhooks)の入出力・対象・制限を確認し、ClaudeとCodexの両方への影響を検証する。
- 配布設定は `.claude-plugin/marketplace.json` と両ツールの `plugin.json`（`.claude-plugin/`・`.codex-plugin/`）。配布仕様や公開手順を変える場合は [更新](README.md#更新)も確認し、両manifestのversionをそろえる。
- 共有設定の変更は複数リポジトリに影響する。特定リポジトリの検証はそのリポジトリの設定に任せ、共通hookに固有のコマンドを増やさない。権限・マージ・公開の許可は、skillの存在や導入だけでは追加されない。

## 検証

- 変更したシェルは `bash -n <対象.sh>`、JSONは `jq empty <対象.json>` で構文確認する。hookの挙動変更は一時ディレクトリと模擬コマンドで対象・除外・入出力を確かめ、利用者のリポジトリを検証用に書き換えない。
- 文書・スキル変更は手順の実行条件、相対リンク、両ツールでの扱いを確認する。作業に応じた `verify-changes` を使う。プラグイン変更に伴う導入・更新は、依頼範囲とREADMEに従う。

## 調査と指示の保守

- `AGENTS.md` は `CLAUDE.md` への相対リンク。本文は一度読み、実体を編集する。
- `rg` は対象ディレクトリから名前・見出し・シンボルを探す。通常は `-g` で依存・成果物・ログ・ロックファイル・生成コードを除外し、依存・生成・型・障害の調査では直接読む。見つからなければ範囲・除外を見直す。
- 必須検証を行い、要点・失敗箇所を報告する。同じ差分・依存・設定・実行条件の結果は再利用する。
- ここは恒久規約・必須条件・主要コマンド・参照先に限る。進捗はチャット・既存Issue/PR、機能・構成・依存・設定等の現在値は元の定義へ。規約・条件・参照先の変更や継続して必要な判断基準の追加時に更新する。
- スキルは説明から選び、該当 `SKILL.md` に従う。一覧・手順は転記せず、このガイドの必須適用条件は守る。
